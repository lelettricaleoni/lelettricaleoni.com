# Journal 2026-09

<!-- distilled-through: 2026-09-22T14:21:38.450Z -->

<!-- ts:2026-09-09T10:57:10.571Z -->
## 2026-09-09 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+19 altri)
**Commit:**
- d04cfeb Merge pull request #18 from lelettricaleoni/feat/flags-discovery-endpoint
- d6caa90 Expose the flags discovery endpoint so the dashboard can see the flags
- 220a0ab Merge pull request #17 from lelettricaleoni/fix/flyover-terrain-anchoring
- c4b9cd0 Never make a request wait on the flags service
- 60cbe60 Cache flag evaluation to undo a 40x slowdown
- 845742e Merge pull request #16 from lelettricaleoni/fix/flyover-terrain-anchoring
- c56325b Surface flag evaluation failures and correct the env example
- a22b88b Move feature flags from environment variables to Vercel Flags
- 404ceaf Add feature flags for the routes section and its media
- 387ffbc Add design spec for the test suite and fake services

<!-- ts:2026-09-10T06:47:51.442Z -->
## 2026-09-10 · be48395
**Branch:** main · **Intento:** —
**Note:**
- La pipeline video NON e' collegata: su MinIO notify_webhook e' enable=off con endpoint
  vuoto, nessuna regola evento sui bucket, e il sito non chiama mai il worker. Un video
  caricato oggi resterebbe in private/ per sempre. I 4 video in produzione sono anteriori
  a questo stato e nel vecchio formato piatto.
- L'utente MinIO dev-lelettrica-website aveva policy "readwrite" = accesso completo a tutti
  i bucket, produzione inclusa. Ristretto con la policy dev-bucket-only; verificato che ora
  la produzione risponda AccessDenied.
- Collegare le notifiche richiede un riavvio di MinIO, e MinIO valida i target all'avvio:
  con un endpoint sbagliato o il worker spento potrebbe non ripartire. Non ho modo di
  verificare il worker (kubeconfig assente).
- CORREZIONE: il worker in esecuzione nel cluster e' la versione PRE-ABR. Una prova reale
  sul bucket di produzione ha prodotto playlist.m3u8 + seg000.ts (formato piatto), non
  master.m3u8. Quindi l'immagine deployata e' anteriore al commit 55cc594 e il repo main
  e' avanti. La previsione "il prossimo caricamento sparira'" era sbagliata: sparira' solo
  quando verra' deployata la versione ABR. La correzione sul sito resta giusta e rende
  sicuro quel deploy futuro.
- Pipeline ora collegata: notify_webhook:videoworker verso
  http://video-worker.minio.svc.cluster.local:8080 + regole s3:ObjectCreated con prefisso
  private/route-videos/ su entrambi i bucket. Provata: transcodifica in ~10 s.
- Il riavvio di MinIO e' andato liscio; il rischio "non riparte con endpoint irraggiungibile"
  non si e' materializzato. mc admin service restart pero' non ritorna: verificare la salute
  con curl /minio/health/live invece di aspettare il comando.
- mc admin config export NON mostra i valori uguali ai default ne' i target nominati: non e'
  affidabile per verificare se una config e' stata scritta. Usare mc admin config get <sub>:<nome>.
- BUG da sistemare: con MinIO irraggiungibile il video appare comunque, con la scritta
  "Video in elaborazione" invece di sparire. Ipotesi (non verificata): la cache Redis
  aggiunta oggi tiene l'URL del manifesto per 7 giorni, quindi resolveHlsUrl restituisce
  un URL valido anche a storage giu'; il video supera il controllo di prontezza, viene
  renderizzato, la riproduzione fallisce e scatta lo stato d'errore del player. Prima della
  cache il controllo falliva e il video veniva filtrato via.
  Due cose da decidere: se un errore di rete debba invalidare la voce in cache, e che
  messaggio mostrare -- "in elaborazione" e' falso quando il problema e' lo storage.
- Migrazione MinIO da k3s a Docker Compose completata. Il piano prevedeva solo `mc mirror`,
  ma quello copia i soli oggetti: sono serviti anche export/import IAM (le secret key non
  sono rileggibili), ricreazione delle notifiche e della policy anonima -- senza
  quest'ultima i video davano 403.
- Traefik di k3s intercettava 80/443 con DNAT del CNI, prima che Docker le vedesse: NPM non
  riceveva nulla e il log del suo host restava vuoto. Diagnosticato dal corpo del 404, che
  era in stile Go e non di nginx.
- Regola OCI inefficace perche' aveva *Source* Port Range 80/443 invece di lasciarlo vuoto:
  un client parte da una porta effimera, quindi non corrispondeva mai. Salvata senza errori.
- Il vecchio MinIO e' caduto durante la migrazione: Longhorn non agganciava piu' il volume.
  Concausa mia: container Docker piu' transcodifica ffmpeg a 3 qualita' su una VM con UNA
  CPU che reggeva la produzione. Da valutare il carico prima di provare su macchine vive.
- k3s-uninstall azzera le iptables, Docker comprese: va riavviato subito dopo, altrimenti
  il sito torna giu'.
- Le immagini Ubuntu di OCI estendono partizione e filesystem da sole al boot (cloud-init):
  growpart/resize2fs a mano servono solo se non si riavvia.
- open-iscsi si puo' rimuovere solo dopo aver verificato che non ci siano sessioni attive:
  su OCI alcuni volumi si agganciano via iSCSI e toglierlo renderebbe la VM non avviabile.
- containerd e ctr NON sono residui di k3s: appartengono a containerd.io, cioe' a Docker.
- Vincolo nuovo da Kevin: la VM clustrenode1 va considerata effimera, "tutto cio che e' sulla VM potrebbe sparire". Ha ribaltato il design della coda.
- Misurato: i dati MinIO sono 17 MB. Il worker fa `mc rm` del sorgente dopo la transcodifica, quindi gli HLS sulla VM sono l'unica copia dei video del sito. Nessun backup. Scoperto mentre progettavo altro.
- `~/docker` sulla VM non e' un repo git: compose, .env, certificati LE, config NPM ed export IAM vivono solo li. L'iam-backup della migrazione sta sulla stessa macchina che puo' sparire, quindi non e' un backup.
- Decisione: la coda dev'essere *ricostruibile*, non *durevole*. Un oggetto in private/route-videos senza HLS corrispondente E' il lavoro da fare: la verita' sta gia' nello storage. Scartato spostare la coda su Upstash — una coda non puo' essere piu' durevole dei dati che serve, i job sopravvissuti sarebbero zombie.
- Verificato (non dedotto): Upstash free = 500k comandi/mese; pay-as-you-go $0.20/100k. BullMQ polla anche a coda vuota (bzpopmin ~5s + stalled check 30s) ≈ 600k/mese: sfora il gratuito senza aver transcodificato niente. Upstash lo documenta e raccomanda un piano fisso.
- Due Redis divisi per *chi legge il dato*, non per velocita': Upstash e' il piu' LENTO dei due (HTTP), ma e' l'unico raggiungibile da Vercel, che non tiene connessioni TCP fra invocazioni. Il locale regge il polling gratis ma non e' raggiungibile dal sito.
- BullMQ ha un port Python ufficiale (taskforce.sh), interoperabile col Node: stessi script Lua. Quindi il worker resta Python, niente riscrittura.
- Cercato prima di dare per buono il worker custom: le alternative sono servizi a pagamento (Mux, Cloudflare Stream) o progetti personali su GitHub. Tdarr/FileFlows lavorano su cartelle, non su eventi S3. Non mancava il transcoder, mancava la coda.
- Kevin ha scelto la card di stato solo per l'admin, non per i visitatori.

<!-- ts:2026-09-10T07:20:57.456Z -->
## 2026-09-10 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+37 altri)

<!-- ts:2026-09-10T07:23:35.787Z -->
## 2026-09-10 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+37 altri)
**Commit:**
- 8f1eeea Serve videos from R2 instead of MinIO

<!-- ts:2026-09-10T07:23:43.681Z -->
## 2026-09-10 · dfc6d9a
**Branch:** feat/videos-on-r2 · **Intento:** —
**Commit:**
- 8f1eeea Serve videos from R2 instead of MinIO

<!-- ts:2026-09-10T07:43:58.693Z -->
## 2026-09-10 · 9433cf1
**Branch:** main · **Intento:** —
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-10T07:44:17.248Z -->
## 2026-09-10 · dd44c15
**Branch:** main · **Intento:** —
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-10T08:03:03.540Z -->
## 2026-09-10 · 9d61232
**Branch:** main · **Intento:** —
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-10T08:03:11.804Z -->
## 2026-09-10 · efee2e0
**Branch:** main · **Intento:** —
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-10T08:03:39.235Z -->
## 2026-09-10 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+48 altri)
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-10T08:04:38.585Z -->
## 2026-09-10 · caf34fa
**Branch:** main · **Intento:** —
**Commit:**
- abf50ff Serve videos from R2 instead of MinIO (#26)

<!-- ts:2026-09-11T07:11:19.898Z -->
## 2026-09-11 · a2c2ca6
**Branch:** feat/content-tests-and-security · **Intento:** —
**Note:**
- Migrazione video MinIO -> R2 completata e verificata in produzione seguendo la catena come il player: pagina -> manifesto -> segmento (200, 3.9 MB, video/mp2t). Zero riferimenti residui a MinIO nella pagina.
- Trappola evitata: la cache HLS su Redis aveva TTL 7 giorni con URL MinIO dentro. Senza cambiare la chiave da v1 a v2 il guasto sarebbe comparso a migrazione "finita", solo su una parte dei video.
- `.env.local` punta ai bucket DEV (dev-lelettrica-trails, dev.lelettricaleoni.com). La produzione e' su Vercel e le variabili marcate Secret NON si scaricano: escono come [SENSITIVE].
- Ci sono DUE progetti Supabase: hhfnhzdourgkinwlqvtc = PRODUZIONE (e' quello a cui punta l'MCP chiamato semplicemente "supabase"), wvyruunbxdqquiarhknt = dev. Il nome piu' corto e' quello piu' pericoloso.
- L'MCP di Cloudflare NON espone la gestione dei token API, nemmeno in lettura, qualunque permesso si conceda in OAuth. Un agente non puo' fabbricare credenziali durature: e' una scelta del server, non un problema di permessi.
- La sandbox dell'MCP Cloudflare blocca le fetch verso domini non-Cloudflare ("requests to X are not allowed"). Non si puo' usare `execute` per travasare dati da un host esterno a R2.
- Upstash supporta le ACL: `ACL SETUSER user on >pw ~prefix:* +set` piu' `ACL RESTTOKEN user pw`. La password deve essere generata con `ACL GENTOKEN <username>`, non scelta a mano. Verificato che il token risultante non legge e non tocca altre chiavi.
- Le chiavi Upstash che contengono slash vanno scritte via POST /pipeline, non via /set/<key>: nel path gli slash sarebbero separatori.
- boto3 e' sincrono: chiamato nudo da codice async blocca l'event loop, BullMQ non rinnova il lock del job e un upload lungo viene dichiarato stalled e ricominciato. Ogni chiamata va in asyncio.to_thread.
- Errore mio: `git reset --hard origin/main` dopo un fetch fallito (remote senza token) ha cancellato modifiche non committate. Il fetch silenzioso e' il pericolo, non il reset.
- Il workflow di build del worker filtrava su `paths: worker.py`. Ristrutturando in moduli l'immagine avrebbe smesso di essere ricostruita senza un solo errore.
- Vercel: il team lelettrica e' su piano **hobby** -> cron limitati a 2 per progetto e uno al giorno. Non sono un'alternativa per lavori piu' frequenti.
- Windmill pubblica immagini arm64 e girerebbe sulla VM, ma vuole Postgres e ~2 GB di RAM su 2 CPU che ffmpeg satura. Kevin ha scelto di estendere il worker esistente.
- Cloudflare Stream: storage prepagato a scatti di $5/mese ogni 1000 minuti, quindi il minimo e' $5/mese anche per 4 video corti. Encoding e ingress gratis. Conviene quando i video crescono.
- Kevin: "usare il meno possibile gli api endpoint nell'applicazione, usare le server actions il piu' possibile". Regola aggiunta a CLAUDE.md con l'eccezione tecnica (un chiamante esterno non puo' invocare una Server Action: l'id e' generato al build).
- MinIO, NPM, volumi, rete proxy-net e cartelle eliminati dalla VM il 2026-09-10: 3,28 GB recuperati, restano solo video-worker e worker-redis, zero porte aperte. Export IAM salvato prima in temp/minio-iam-export-2026-09-10.zip.
- Trappola: `mc du l/` ha riportato "0B 0 objects" mentre `mc ls --recursive` mostrava 8 oggetti da 17 MB. Non fidarsi di quel conteggio per decidere se un bucket e' vuoto.
- `mc admin cluster iam export` NON scrive su stdout: salva un file <alias>-iam-info.zip nella working directory del processo. Redirigere lo stdout produce uno zip fasullo da 55 byte.
- La zona kevinleoni.org non e' nell'account Cloudflare "Lelettrica": l'MCP vede solo lelettricaleoni.com, quindi i record cluster-bucket restano a Kevin.
- Barra unica per i media nel pannello: il video entra in lista una volta sola, perche' la chiave la conia la presigned URL prima dell'upload. Prima viveva in due liste (UploadingItem -> MediaItem) e fra le due c'era un buco visibile: file su R2 ma worker non ancora accorto.
- La logica della barra sta in lib/media-progress.ts, fuori dal JSX, cosi' e' testabile: un test percorre l'intero viaggio e verifica che la percentuale non torni mai indietro. Le due meta' (upload / elaborazione) non affermano nulla sulla durata delle fasi.
- Conseguenza non ovvia: il campo hidden del form ora esclude gli item a meta' upload, altrimenti salvare in anticipo registrerebbe un video inesistente.
- RECIDIVA: ho di nuovo pushato dritto su main (247dbec, una riga di ROADMAP) usando l'esenzione amministratore, dopo averlo gia' fatto il 2026-09-09 e averlo annotato come errore. Il pattern e': commit "piccolo" di documentazione a fine lavoro, quando la PR sembra sproporzionata. Non lo e': oggi ne ho aperte sei e costano 40 secondi di CI.
- Regola nuova da Kevin: nessun dettaglio infrastrutturale nei testi che l'utente legge. Nate da due righe reali: "Foto su R2, video su MinIO" nel form percorsi (falsa da 24 ore) e "Foto e video su R2..." che avevo scritto io un'ora prima. Poi ne ha trovate altre due su Azure che la mia prima ricerca non copriva.
- Lezione sulla ricerca: cercavo R2|MinIO|bucket|worker|Upstash|Supabase|Cloudflare|HLS e mi ero convinto di aver finito. Azure non era nell'elenco. Quando si ripulisce una categoria di stringhe, l'elenco dei termini va costruito dai servizi che il progetto usa davvero (leggere .env.local), non da quelli che vengono in mente.
- Il pannello admin ha testi misti italiano/inglese ("Content (Italian)", "Description *", "No routes yet" accanto a "Trascina il video qui"). Non chiesto, non toccato, ma e' un'incoerenza visibile.
- Misurato il peso reale dei video ABR: 93 s = 72 MiB, cioe' 46 MiB/minuto (1080p 55%, 720p 29%, 480p 16%). La mia stima di stamattina ("10 GB = ~650 video") era sbagliata di un fattore 4: partiva dai 4 video esistenti, corti e a qualita' singola. 10 GB = ~222 minuti.
- Kevin vuole tenere le tre rendition: l'esperienza del cliente viene prima dello spazio. Chiarito che CRF non significa meno qualita' ma meno byte a parita' di resa — resta in roadmap come opzione da misurare, non da imporre.
- Kevin stava valutando di migrare tutto a MinIO proprio per il timore dello spazio, poi si e' fermato riconoscendo che Cloudflare va meglio. I numeri lo confermano: su OCI lo spazio oltre i 200 GB costa il triplo di R2 e non e' replicato.
- Trappola npm 10.9.8: `npm update` e ogni `npm install` con molti pacchetti falliscono con "Cannot read properties of null (reading 'edgesOut')". Sopravvive a rm -rf node_modules E alla rigenerazione del package-lock (anzi, senza lock fallisce pure `npm install` nudo). Rimedio: installare a blocchi di 4-6 pacchetti. vitest lo fa scattare da solo, quindi e' rimasto a 4.1.7.
- Errore mio nel verificare il player: `document.querySelector('video')` prendeva la miniatura in autoplay della card, non il player nel lightbox. Ho quasi concluso che 37 aggiornamenti avessero rotto il player. I player veri si riconoscono dalla sorgente blob: (MSE). Misurare l'elemento giusto prima di dedurre.
- Idea di Kevin, implementata: la ritraduzione EN/DE si decide confrontando il testo italiano salvato con quello in arrivo, invece che da un interruttore. Prima, dimenticare l'interruttore lasciava le altre due lingue disallineate in silenzio.
- Metodo, non tecnica: ho cambiato @flags-sdk/vercel avanti e indietro tre volte inseguendo un 500 in dev, perche' installavo pacchetti MENTRE il dev server girava. Il server serviva i moduli vecchi, quindi nessuna delle prove era controllata. La causa era `.next` stantio (terza volta oggi). Regola: per confrontare due versioni di una dipendenza — fermare il server, installare, cancellare .next, riavviare. Altrimenti si misura rumore.
- Falsa pista: l'avviso "Can't resolve @vercel/flags-definitions" appare in dev con qualunque versione di @vercel/flags-core (1.8.0 e 1.8.1) e quel pacchetto non esiste su npm (404). E' rumore preesistente, non la causa di un 500.
- Le card dei percorsi usano ora grid-rows-subgrid: prendono le loro quattro fasce dalla griglia genitore, quindi si allineano fra card della stessa fila. Il gap verticale del genitore va a zero (sotto subgrid si applicherebbe DENTRO la card) e lo spazio fra card lo mette la card con mb-6.
- Suite di test browser (fase 1, geometria) implementata. Provata NEI DUE SENSI prima di committare: contro una build del codice rotto segnala "4 elementi fuori dal contenitore" e fallisce a tutte e tre le larghezze; contro quello corretto 18 test passano. Un test che non l'ho visto fallire sul bug che dovrebbe prendere non vale niente.
- Falso positivo trovato calibrando: il pulsante del menu mobile sborda di 4px per un -mr-1 voluto (margine negativo per allineare l'icona al bordo ottico). Tolleranza portata a 8px — un ottavo dei 67px che sbordavano le card rotte.
- Trappola grossa: un preview Vercel protetto risponde con la PAGINA DI LOGIN di Vercel, che ha un layout perfetto. Il test passava misurando quella. Ora c'e' una guardia esplicita su vercel.com/login.
- page.evaluate serializza la funzione ed esegue nel browser: nulla del modulo TS e' in scope. Una costante del file (TOLERANCE) dava ReferenceError, e il test falliva per quello sembrando che avesse trovato il bug. Passare i valori come argomenti.
- La pagina /it/routes in dev NON completa la navigazione (ne' load ne' domcontentloaded scattano): Playwright e Chrome vanno in timeout mentre curl risponde. I test browser vanno eseguiti contro `next start` o un deploy, non contro `next dev`.
- vitest senza config raccoglieva anche tests/browser/ e falliva sugli import di Playwright. Serve vitest.config.ts con include/exclude espliciti quando convivono due runner.
- Immagini: `sizes` usava unita' vw ma il contenitore e' max-w-6xl, quindi oltre ~1200px la card resta FISSA a 313px. Misurato: a 768 chiedeva w=1920 per una card da 290px (6.6x), a 1920 chiedeva w=1920 per 313px. Corretto con calc() sui breakpoint reali e 340px fisso oltre: ora 640/750 invece di 1080/1920. Lista da 911 KB a 369 KB su telefono.
- Regola generale: quando il contenitore ha un max-width, `vw` in `sizes` e' lo strumento sbagliato — sovrastima sempre, e di quanto dipende dal viewport.
- Correzione a una mia affermazione: avevo detto a Kevin che l'ottimizzazione immagini era il sospettato per il 25% di Fluid Active CPU. Su Vercel le trasformazioni immagine si fatturano a parte, NON come Active CPU. Non l'avevo verificato prima di dirlo.
- ERRORE GRAVE ripetuto: tre commit finiti su main perche' ero convinto di essere sul branch di una PR e non ho guardato `git branch --show-current`. Terza volta oggi. Risolto rendendo la cosa impossibile invece che affidarla alla memoria: .githooks/pre-commit rifiuta i commit su main/staging, attivo con `git config core.hooksPath .githooks`. Regola aggiunta a CLAUDE.md.
- `git add -A` raccoglie i worktree degli agenti in `.claude/worktrees/` come repository annidati. Vanno in .gitignore prima di lanciare agenti in parallelo.
- Bug di sicurezza trovato da un agente e verificato da me: app/auth/callback/route.ts controllava `user_metadata.role` mentre proxy.ts, getAdminUser() e loginAction usano `app_metadata.role`. user_metadata e' scrivibile dall'utente stesso via supabase.auth.updateUser, quindi era un'escalation. Tre punti su quattro erano giusti: e' l'incoerenza che lo rendeva invisibile.
- Il check `browser` blocca tutte e 11 le PR aperte. Ultimo stato: 33/36 passano, il preview e' raggiungibile col token. I 3 falliti sono lo stesso caso (h1 vuoto sul dettaglio, a tre larghezze). Ipotesi non verificata: percorso senza riga in route_translations sul DB che il preview interroga.
- Scoperto: le PR di Dependabot NON eseguono davvero i test browser — non ricevono i secret, il workflow si salta da solo e riporta success. Un verde su quelle PR non prova niente.
- Scoperto: codeql.yml esiste solo sul branch #50, non su main. Quello che gira e' il default setup di GitHub con suite ristretta. Lo zero segnalazioni e' autentico ma e' lo zero di QUELLA suite: security-extended e le query di qualita' non hanno mai girato.
- Trappola workflow: `bash -e` piu' un `gh api` che fallisce = il passo muore a meta' ciclo con un exit code strano (47). Servono `|| true` sulle chiamate e i `permissions` espliciti, altrimenti il token non ha lo scope `deployments`.
- Trappola: pretendere un 200 secco da un preview e' sbagliato — il preview reindirizza al proprio URL canonico e proxy.ts reindirizza per conto suo. Tre PR dichiarate irraggiungibili per dieci minuti ciascuna.

<!-- ts:2026-09-11T09:33:01.005Z -->
## 2026-09-11 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+76 altri)
**Commit:**
- ae2abca Pannello per gli account amministratori (#51)
- 1ae2bcb Give every page a ceiling it has to stay under (#52)
- 1f77e8c Close the open Dependabot alerts in one lockfile npm ci accepts (#63)
- 6155b1b Reach the database through the transaction pooler (#64)
- e2c37e7 Hand the database connection back when an instance goes idle (#62)
- fcb977e ci: bump actions/checkout from 4 to 7 (#55)
- 32997d0 ci: bump actions/upload-artifact from 4 to 7 (#54)
- 8e4d6ca Declare the token scope the verify job actually needs (#53)
- 200b9fb Assert what each page shows, and set the repository to watch itself (#50)
**Note:**
- 2026-09-11 Test browser: tolto ogni 'networkidle'. I video HLS delle card scaricano segmenti finché la pagina è aperta, quindi la rete non è mai ferma: ogni visita pagava 15 s, e il test del dettaglio (due visite) finiva il tempo e riportava la pagina chiusa come "h1 vuoto". L'ipotesi delle traduzioni mancanti era sbagliata. Suite da ~5 min a 21 s.
- 2026-09-11 CodeQL: tenuto il default setup di GitHub con query_suite=extended, cancellato codeql.yml. I due non convivono (GitHub rifiuta le analisi "advanced" se il default è acceso), e il default si aggiorna da solo. Persa la suite "quality", che nel default setup non esiste.
- 2026-09-11 PR di Dependabot chiuse in blocco: i loro lockfile erano incompleti (mancava esbuild 0.28.2) e `npm ci` falliva. Sostituite da una PR unica con lockfile generato in locale.
- 2026-09-11 INCIDENTE: /it/routes in produzione in errore ("This page couldn't load", EMAXCONNSESSION). Produzione e tutte le preview condividono il pooler Supabase in session mode, 15 slot, uno per istanza calda, e il client postgres non aveva idle_timeout: sei PR testate insieme hanno tenuto occupati tutti gli slot. Ripristinato chiudendo con pg_terminate_backend le 14 sessioni Supavisor idle da >30 s (i client si riconnettono da soli). Correzioni: idle_timeout 20 (#62) e, da fare nel pannello Vercel, DATABASE_URL su porta 6543 (transaction mode) per Production e Preview.
- 2026-09-11 Il test di contenuto ha preso l'incidente prima di chiunque: è la ragione per rendere `browser` obbligatorio. Ma i test stessi, con più PR in parallelo, sono carico sul database di produzione — vale la pena valutare di puntare le preview al database di sviluppo.
- 2026-09-11 Lockfile (dall'agente di #63): npm 10.9.8 va in `edgesOut` anche installando vitest da solo, serve `npx npm@11` per quel passo; ma npm 11 toglie dal lockfile l'esbuild opzionale di vite e npm 10 poi lo rifiuta — è l'errore di tutte le PR npm di Dependabot. Il lockfile finale va scritto e verificato con npm 10 (`npm ci` da zero). L'override su `@esbuild-kit/core-utils` è stato applicato solo togliendo a mano la copia di esbuild annidata.
- 2026-09-11 Budget di prestazione sul preview: una risposta per caricamento non termina mai (probabilmente la toolbar di Vercel, assente in produzione), e request.sizes() la aspettava per sempre. Ogni misura ha ora un tetto di 2 s.
- 2026-09-11 maplibre-gl non è importato da nessun file: la vulnerabilità critica stava in una dipendenza morta.

<!-- ts:2026-09-11T09:36:50.231Z -->
## 2026-09-11 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+76 altri)
**Commit:**
- ae2abca Pannello per gli account amministratori (#51)
- 1ae2bcb Give every page a ceiling it has to stay under (#52)
- 1f77e8c Close the open Dependabot alerts in one lockfile npm ci accepts (#63)
- 6155b1b Reach the database through the transaction pooler (#64)
- e2c37e7 Hand the database connection back when an instance goes idle (#62)
- fcb977e ci: bump actions/checkout from 4 to 7 (#55)
- 32997d0 ci: bump actions/upload-artifact from 4 to 7 (#54)
- 8e4d6ca Declare the token scope the verify job actually needs (#53)
- 200b9fb Assert what each page shows, and set the repository to watch itself (#50)

<!-- ts:2026-09-14T11:12:48.448Z -->
## 2026-09-14 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+108 altri)
**Commit:**
- 571fe72 Give the homepage something to say about routes
- 0dbc1c0 Give drizzle-kit a baseline it can actually track (#80)
- 72fb1a7 Drop the surface field from routes entirely (#79)
- 59c0675 Drop maplibre-gl: nothing imports it (#78)
- bc7fa38 Add Claude Code GitHub Workflow (#77)
- fa8338c Drop two roadmap items that shipped (#76)
- d36debe Let the list switch between photos/video and the GPX map (#75)
- d458e14 Record why R2 CORS blocked every upload from a preview (#73)
- ddad00f Give environments and their variables a real home (#72)
- be1a150 Close four Dependabot PRs with one lockfile npm ci accepts (#70)
**Note:**
- 2026-09-14 Video/foto/GPX non si caricavano su Preview dopo l'isolamento del bucket R2: dev-lelettrica-trails aveva CORS solo per localhost:3000, mai esposto a un'origine *.vercel.app prima d'ora. R2 accetta un carattere jolly nell'origine (verificato con una richiesta PUT reale, non solo il preflight): aggiunta la regola https://lelettricaleoni-*-lelettrica.vercel.app invece di un *.vercel.app generico che avrebbe accettato l'origine di qualunque progetto Vercel altrui.

<!-- ts:2026-09-14T12:01:29.334Z -->
## 2026-09-14 · 81fa78b
**Branch:** feat/home-routes-teaser · **Intento:** —
**Commit:**
- 571fe72 Give the homepage something to say about routes
- 0dbc1c0 Give drizzle-kit a baseline it can actually track (#80)
- 72fb1a7 Drop the surface field from routes entirely (#79)
- 59c0675 Drop maplibre-gl: nothing imports it (#78)
- bc7fa38 Add Claude Code GitHub Workflow (#77)
- fa8338c Drop two roadmap items that shipped (#76)
- d36debe Let the list switch between photos/video and the GPX map (#75)
- d458e14 Record why R2 CORS blocked every upload from a preview (#73)
- ddad00f Give environments and their variables a real home (#72)
- be1a150 Close four Dependabot PRs with one lockfile npm ci accepts (#70)

<!-- ts:2026-09-14T12:36:57.123Z -->
## 2026-09-14 · 309dd4a
**Branch:** main · **Intento:** riesci a documentare lo stato attuale del progetto, poi bisognerebbe documentare anche visioni future e funzionalità future, non so se esiste un sistema comodo per farlo, ma vorrei che questa document…
**File:** docs/superpowers/specs/2026-09-09-ai-docs-system-design.md, next.config.ts, lib/terrain.test.ts, lib/terrain.ts, components/route-flyover.tsx, CLAUDE.md (+110 altri)
**Commit:**
- 7046520 Drop the maplibre-gl debt entries now that it's gone (#82)
- b624896 Give the homepage something to say about routes (#81)
- 0dbc1c0 Give drizzle-kit a baseline it can actually track (#80)
- 72fb1a7 Drop the surface field from routes entirely (#79)
- 59c0675 Drop maplibre-gl: nothing imports it (#78)
- bc7fa38 Add Claude Code GitHub Workflow (#77)
- fa8338c Drop two roadmap items that shipped (#76)
- d36debe Let the list switch between photos/video and the GPX map (#75)
- d458e14 Record why R2 CORS blocked every upload from a preview (#73)
- ddad00f Give environments and their variables a real home (#72)

<!-- ts:2026-09-14T14:13:27.476Z -->
## 2026-09-14 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+13 altri)
**Commit:**
- 3c22011 Force genuine per-request flag evaluation with connection()
- b6a33d9 Record live preview measurements in the plan
- ec8a93f Record Task 10 progress: build/test verified, PR #84 open, preview measurement blocked on access
- bbb8a07 Record Tasks 6-9 progress in the plan, correcting the getFlags/use-cache design
- 96d3948 Invalidate the routes cache on publish instead of a dead revalidatePath
- 39cc186 Cache the routes list and detail pages' DB/R2 work, not the flag check
- f938116 Record Tasks 4-5 progress in the plan
- bf997ad Wrap useSearchParams in Suspense on both update-password pages
- de3c74b Give /manage its own root layout now that app/layout.tsx is gone
- 98bb79c Move the root layout under app/[lang], off headers()
**Note:**
Under Next 16 Cache Components, a raw Date.now()/new Date() read during prerendering fails the build even on a route marked `instant = false` — that opt-out covers uncached IO/runtime APIs, not synchronous non-determinism. lib/flags.ts's staleness check (Date.now() - cache.at) blocked every page calling getFlags() that wasn't already fully dynamic for another reason. Fixed by switching to performance.now() (only ever used for an elapsed delta, never a wall-clock value) — resolves it once for every caller instead of per-route. Found via `npm run build -- --debug-prerender`, which names the exact failing line.

<!-- ts:2026-09-15T10:59:05.392Z -->
## 2026-09-15 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+40 altri)
**Commit:**
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)
- b58b4cc Show the stats band with dashes instead of collapsing it (#89)
- d906772 Distill the routes-caching work into STATE.md and ROADMAP.md (#85)
- 874b4c8 Unify the routes list card's empty/loading placeholder (#90)
- 83d56bc Start preview videos at the lowest quality rendition, not an optimistic guess (#88)

<!-- ts:2026-09-15T12:55:10.802Z -->
## 2026-09-15 · 752b10f
**Branch:** feat/dev-tools-panel · **Intento:** —
**Commit:**
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)
- b58b4cc Show the stats band with dashes instead of collapsing it (#89)
- d906772 Distill the routes-caching work into STATE.md and ROADMAP.md (#85)
- 874b4c8 Unify the routes list card's empty/loading placeholder (#90)
- 83d56bc Start preview videos at the lowest quality rendition, not an optimistic guess (#88)

<!-- ts:2026-09-15T13:03:26.254Z -->
## 2026-09-15 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+40 altri)
**Commit:**
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)
- b58b4cc Show the stats band with dashes instead of collapsing it (#89)
- d906772 Distill the routes-caching work into STATE.md and ROADMAP.md (#85)
- 874b4c8 Unify the routes list card's empty/loading placeholder (#90)
- 83d56bc Start preview videos at the lowest quality rendition, not an optimistic guess (#88)

<!-- ts:2026-09-15T13:04:59.928Z -->
## 2026-09-15 · 6d1ada0
**Branch:** feat/dev-tools-panel · **Intento:** —
**Commit:**
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)
- b58b4cc Show the stats band with dashes instead of collapsing it (#89)
- d906772 Distill the routes-caching work into STATE.md and ROADMAP.md (#85)
- 874b4c8 Unify the routes list card's empty/loading placeholder (#90)
- 83d56bc Start preview videos at the lowest quality rendition, not an optimistic guess (#88)

<!-- ts:2026-09-15T13:48:24.623Z -->
## 2026-09-15 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+48 altri)
**Commit:**
- 5d8f58e Fix the R2 analytics query: can't order by an unselected field
- 02bc89b Show CPU utilization, not core count, on the dev-tools page
- a817340 Fill in the R2 storage stats on the dev-tools page
- f30c313 Bound every external call in dev-stats.ts with a timeout
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)

<!-- ts:2026-09-15T13:49:10.557Z -->
## 2026-09-15 · 0a25b3a
**Branch:** feat/dev-tools-panel · **Intento:** —
**Commit:**
- 5d8f58e Fix the R2 analytics query: can't order by an unselected field
- 02bc89b Show CPU utilization, not core count, on the dev-tools page
- a817340 Fill in the R2 storage stats on the dev-tools page
- f30c313 Bound every external call in dev-stats.ts with a timeout
- 04d822a Merge main (manage home page, gallery placeholder) into dev-tools-panel branch
- 1a0f6a0 Add a permission-gated dev-tools page to the panel
- 7377bcc Reuse the same media placeholder in the route gallery (#91)
- 2a3fb25 Give /manage a real home instead of redirecting away (#94)
- e5d66e4 Raise the DB client's connection pool from 1 to 3 (#93)
- 25eebc0 Add the dev-tools panel page to the roadmap (#92)

<!-- ts:2026-09-17T11:33:21.428Z -->
## 2026-09-17 · 6fa4e0b
**Branch:** feat/generalize-media-table · **Intento:** —
**Commit:**
- 00161c4 Generalize route_photos into a shared media table
- e4d17cc Add pure price-for-day calculator for bike categories (#120)
- 8411adc Complete the Bike Options admin menu (Tasks 6-7) (#119)
- 2bd45a8 Plan correction: Task 4 must wait on the real browser CI check (#118)
- ed938df Add bike_models, translations, and size/version junction tables (#117)
- 880fac5 Add bike_sizes, bike_versions, bike_categories tables (#116)
- 4940038 Implementation plan: bike models catalog and shop inventory (#115)
- 1f115ba Design spec: bike models catalog and shop inventory (#114)

<!-- ts:2026-09-17T12:32:13.658Z -->
## 2026-09-17 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+92 altri)
**Commit:**
- 4eabf99 Add bike admin pages to the sidebar navigation
- f4cc995 Add /manage/bikes/shop admin page
- 8a23588 Add server actions for shop inventory (bike units)
- 52494d7 Merge remote-tracking branch 'origin/main' into feat/bike-units-shop
- d75810d Bike models catalog — Models phase (Tasks 8-11) (#123)
- d9b5b9a Add bike_units table for shop inventory
- 56bcff0 Add journal entry for the bike-options and media-generalization work (#122)
- 7e526ef Generalize route_photos into a shared media table (#121)
- e4d17cc Add pure price-for-day calculator for bike categories (#120)
- 8411adc Complete the Bike Options admin menu (Tasks 6-7) (#119)
**Note:**
- Durante la verifica dal vivo del Task 11 (form modelli bici), la prima connessione al pooler Postgres dev di un processo `next dev` appena avviato ha dato ECONNRESET ripetuto per alcuni minuti, poi si è risolta da sola; una volta stabilita una connessione sana (da un'altra richiesta concorrente), le richieste successive sono andate a segno normalmente. Riprodotto anche fuori da Next con uno script tsx isolato: stesso errore, stessa risoluzione spontanea. Sembra un problema di rete/TLS nella primissima connessione del processo verso il pooler Supabase (aws-1-eu-central-1.pooler.supabase.com:6543), non un bug applicativo — nessuna query coinvolta è N+1 o anomala. Non richiede azione di codice, ma se si ripresenta spesso vale la pena controllarlo.
- Piano bike-models-and-inventory: Task 3 (tabella bike_units) era stato saltato per errore nell'ordine di esecuzione — implementato su branch feat/bike-units-shop il 2026-09-17, migrazione (0005_kind_microchip.sql) applicata a dev e produzione via Supabase MCP.
- Piano bike-models-and-inventory completo (Task 1-14, PR #113-124): opzioni globali, catalogo modelli, inventario negozio. Vale un puntatore in ROADMAP.md sotto "Decisioni passate ancora rilevanti" verso `docs/superpowers/specs/2026-09-17-bike-models-and-inventory-design.md` e il relativo piano, come già fatto per gli altri lavori chiusi — non ancora fatto, da fare al prossimo /distill o manualmente dopo il merge di PR #124.

<!-- ts:2026-09-21T07:09:30.462Z -->
## 2026-09-21 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+92 altri)
**Commit:**
- a852eca Point to the bike models/inventory spec from STATE.md, journal entry (#127)

<!-- ts:2026-09-21T07:35:43.614Z -->
## 2026-09-21 · 5c04f94
**Branch:** main · **Intento:** claude
**Commit:**
- a852eca Point to the bike models/inventory spec from STATE.md, journal entry (#127)

<!-- ts:2026-09-21T09:03:51.153Z -->
## 2026-09-21 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+106 altri)
**Commit:**
- dce4180 Add one-off sha256 backfill script for photos and GPX files
- ad122dc SHA-256 for uploaded files — Tasks 1-10 (schema through UI) (#130)
- 34404b4 Implementation plan: SHA-256 for uploaded files (#129)
- f9b3ed1 Design spec: SHA-256 for every uploaded file (#128)
- a852eca Point to the bike models/inventory spec from STATE.md, journal entry (#127)
**Note:**
- Il worker video (jobs/transcode.py in videoStream-bucketWorker) cerca i sorgenti solo in `private/route-videos/` (SOURCE_PREFIX fisso): i video dei modelli di bici (`private/bike-model-videos/...`, aggiunti nel Task 9 del piano bike-models-and-inventory) non vengono ancora trascodificati. Non è un bug: la transcodifica per i video dei modelli bici è semplicemente lavoro non ancora fatto. Da riprendere in una sessione futura — probabilmente generalizzando SOURCE_PREFIX/OUTPUT_PREFIX a una lista di coppie invece di un valore fisso.
- Kevin ha chiesto (2026-09-21) il riordino drag & drop dei percorsi nella lista admin, con l'ordine mantenuto anche nella lista pubblica. Deciso esplicitamente di farlo DOPO il lavoro su SHA-256/GPX, in un giro di brainstorming separato — non bundlarlo. Probabile forma: colonna `display_order` su `routes` (stesso pattern già usato per `bike_sizes`/`bike_categories`), drag reorder con @dnd-kit (già dipendenza, usato in `media-upload.tsx`), da rispettare sia in `getRoutesForAdmin` sia in `getRoutesListData`/la pagina pubblica `/routes` — verificare l'interazione con il tag cache `routes-list`.
- Trovato durante la verifica dal vivo del Task 7 (piano upload-sha256): `MediaUpload` produce un warning di hydration mismatch su `aria-describedby="DndDescribedBy-N"` quando si naviga più volte client-side tra pagine con `DndContext`/`useSortable` (@dnd-kit) nella stessa sessione browser — il contatore usato per generare quell'id sembra accumularsi lato client tra navigazioni mentre il server riparte sempre da zero a ogni richiesta. Preesistente, non introdotto da questo piano (MediaUpload usava già dnd-kit prima). Solo un warning React ("won't be patched up"), nessun impatto funzionale osservato. Da investigare se ricapita o infastidisce.

<!-- ts:2026-09-21T11:39:44.710Z -->
## 2026-09-21 · f72b113
**Branch:** feat/backfill-sha256 · **Intento:** —
**Commit:**
- dce4180 Add one-off sha256 backfill script for photos and GPX files
- ad122dc SHA-256 for uploaded files — Tasks 1-10 (schema through UI) (#130)
- 34404b4 Implementation plan: SHA-256 for uploaded files (#129)
- f9b3ed1 Design spec: SHA-256 for every uploaded file (#128)
- a852eca Point to the bike models/inventory spec from STATE.md, journal entry (#127)

<!-- ts:2026-09-21T13:22:03.372Z -->
## 2026-09-21 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+111 altri)
**Commit:**
- ae350b5 Add drag-and-drop reordering to the admin routes list (#135)
- 6565d5f deps: bump react-dropzone from 15.0.0 to 20.1.2 (#126)
- f9e1cf2 deps: bump the patch-e-minor group with 11 updates (#125)
- 44754bc Translate remaining Italian text in the admin panel to English (#134)
- 2e6b902 Group admin sidebar into categories, fix Italian label (#133)
- 3d9eb14 Point to the upload-sha256 spec from STATE.md, journal entry (#132)
- 874d97e Add one-off sha256 backfill script for photos and GPX files (#131)
- ad122dc SHA-256 for uploaded files — Tasks 1-10 (schema through UI) (#130)
- 34404b4 Implementation plan: SHA-256 for uploaded files (#129)
- f9b3ed1 Design spec: SHA-256 for every uploaded file (#128)
**Note:**
- Kevin ha confermato (2026-09-21) che in futuro `/manage/login` e `/manage/update-password` (oggi admin-only) andranno unificate con le versioni pubbliche multilingua già esistenti (`/[lang]/login`, già sul sistema vero messages/*.json; `/[lang]/update-password`, multilingua ma con un dizionario inline invece di messages/*.json) — perché i futuri utenti normali (clienti che prenotano) useranno login/cambio password per accedere al proprio profilo. Per ora resta così com'è (`/manage/update-password` tradotta in inglese, non indicizzata, solo admin) — l'unificazione è un lavoro architetturale a sé, da affrontare con un brainstorming dedicato quando si arriva al sistema di prenotazioni. Non ci sono altre pagine account duplicate oltre a questa coppia — `/manage/users` è gestione staff, concettualmente diversa, resta admin-only per sempre.

<!-- ts:2026-09-22T10:08:52.257Z -->
## 2026-09-22 · 288e2bd
**Branch:** main · **Intento:** okay possiamo valutare quali siano gli step successivi, che implementazion ci mancano ?
**File:** docs/ai/STATE.md, docs/ai/journal/2026-09.md, docs/superpowers/specs/2026-09-14-routes-caching-cache-components-design.md, docs/superpowers/plans/2026-09-14-routes-caching-cache-components.md, next.config.ts, app/[lang]/routes/page.tsx (+143 altri)
**Commit:**
- 7dbeab4 Add elevation chart labels to UI dictionaries (IT/EN/DE)
- cc746e4 Wire the elevation chart into the flyover, synced without per-frame React state
- b7289d0 Add RouteElevationChart, a presentational elevation/distance chart
- 78e8064 Add shadcn Chart and Collapsible components
- 3b3737c Share difficulty hex colors between the flyover and the elevation chart
- a358f3a Extract haversineKm into a shared, client-safe lib/geo.ts
- 99cc091 Implementation plan: elevation chart for the 3D flyover (#142)
- d3ac364 Design spec: elevation chart for the 3D flyover (#141)
- ec422db Translate the shop page title to English (#140)
- ef03629 Add the public bikes catalog page (#139)
**Note:**
- PR #137 (spec bici pubbliche, solo doc, zero diff di codice da main): il check 'browser' è fallito una volta su 'il tedesco rende in tedesco' (/de/routes), errore 'locator(main) resolved to 2 elements' — un secondo <main> con testo 'Empfohlene Routen...Ausgewählte'. Rilanciato lo stesso identico commit: verde alla seconda. Probabile flakiness legata a Cache Components/PPR sotto carico dei due worker Playwright in parallelo, non una regressione (main non aveva codice diverso tra i due run). Non indagato oltre, fuori scope dalla sessione in corso — da tenere d'occhio se ricompare.
- Flag "bikes" (PR #139): creato spento ovunque tranne sviluppo come da richiesta di Kevin, ma questo ha fatto fallire il check 'browser' in CI perché gira contro il deployment di preview — la lista bici risultava vuota per design, non per un bug. Corretto accendendo il flag solo in **preview** (per permettere la revisione reale della PR sul deployment), lasciando **produzione spenta** come richiesto. Pattern da riusare per il prossimo flag 'lanciato spento': preview acceso per la revisione, produzione spenta fino all'accensione deliberata.
  **Causa reale del fallimento persistito su 3 tentativi**: non un bug di codice — `update_flag` conferma il nuovo valore istantaneamente via API di gestione, ma la valutazione live (il servizio che le funzioni effettivamente interrogano, verosimilmente via Edge Config) ha impiegato **~15-20 minuti** per propagare il cambio da Off a On per un flag appena creato. Verificato escludendo ogni altra causa: `/it/routes` funzionava sullo stesso deployment nello stesso run (quindi non era un problema generale di rendering/caching), nessun errore nei runtime log, il database dev/preview aveva già il modello pubblicato con l'unità in garage. Il quarto tentativo, a distanza di tempo sufficiente dall'update del flag, è passato senza altre modifiche. **Per il prossimo flag che nasce spento e va acceso in preview per la revisione di una PR: aspettarsi questo ritardo, non fidarsi della risposta immediata di `update_flag`/`get_flag` come prova che la valutazione live sia già aggiornata.**
- Il test flaky "il tedesco rende in tedesco" (/de/routes, due <main>) è ricomparso una terza volta (PR #140, fix di una sola riga su una pagina admin — impossibile che sia causa del codice). Tre occorrenze indipendenti su PR diverse (spec bici #137, #139 no, #140) suggeriscono che non sia rumore isolato ma un pattern reale sotto carico dei worker Playwright paralleli. Da investigare seriamente in una sessione dedicata invece di continuare solo a rilanciare.

<!-- ts:2026-09-22T14:21:38.450Z -->
## 2026-09-22 · 276c893
**Branch:** feat/flyover-elevation-chart · **Intento:** —
**Commit:**
- 7dbeab4 Add elevation chart labels to UI dictionaries (IT/EN/DE)
- cc746e4 Wire the elevation chart into the flyover, synced without per-frame React state
- b7289d0 Add RouteElevationChart, a presentational elevation/distance chart
- 78e8064 Add shadcn Chart and Collapsible components
- 3b3737c Share difficulty hex colors between the flyover and the elevation chart
- a358f3a Extract haversineKm into a shared, client-safe lib/geo.ts
- 99cc091 Implementation plan: elevation chart for the 3D flyover (#142)
- d3ac364 Design spec: elevation chart for the 3D flyover (#141)
- ec422db Translate the shop page title to English (#140)
- ef03629 Add the public bikes catalog page (#139)
